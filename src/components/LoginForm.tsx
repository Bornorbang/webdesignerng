"use client";
import Link from"next/link";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";import{ApiError,api,storeToken}from"@/lib/api";
export default function LoginForm(){const router=useRouter(),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);try{const result=await api<{token:string}>("/auth/login",{method:"POST",body:JSON.stringify({login:form.get("login"),password:form.get("password"),device_name:"WDN website"})});storeToken(result.token,form.get("remember")==="on");const next=new URLSearchParams(window.location.search).get("next");router.replace(next?.startsWith("/")&&!next.startsWith("//")?next:"/profile/")}catch(exception){setError(exception instanceof ApiError?exception.message:"Unable to sign in right now.")}finally{setBusy(false)}}return <form className="auth-form" onSubmit={submit}>{error&&<p className="form-message form-error" role="alert">{error}</p>}<label>Email or Username<input name="login" autoComplete="username" required placeholder="you@example.com"/></label><label>Password<input type="password" name="password" autoComplete="current-password" required placeholder="Enter your password"/></label><div className="auth-form-row"><label className="auth-check"><input name="remember" type="checkbox" defaultChecked/> Remember me</label><Link href="/forgot-password/">Forgot password?</Link></div><button type="submit" disabled={busy}>{busy?"Signing inâ€¦":"Sign In"}<span>â†’</span></button></form>}

