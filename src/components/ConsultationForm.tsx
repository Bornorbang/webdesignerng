"use client";
import{FormEvent,useState}from"react";import{ApiError,api}from"@/lib/api";
const prices:Record<string,string>={"20":"30,000","40":"50,000","60":"70,000"};
export default function ConsultationForm(){
 const[duration,setDuration]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);
 async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);try{const result=await api<{authorization_url:string}>("/consultations",{method:"POST",body:JSON.stringify({full_name:form.get("full_name"),email:form.get("email"),phone:form.get("phone"),consultation_type:form.get("consultation_type"),preferred_date:form.get("preferred_date")||null,preferred_time:form.get("preferred_time")||null,project_description:form.get("project_description")||null,inspiration:form.get("inspiration")||null,session_duration:form.get("session_duration")})});if(!result.authorization_url)throw new Error("Payment link missing");window.location.assign(result.authorization_url)}catch(exception){setError(exception instanceof ApiError?Object.values(exception.errors).flat()[0]??exception.message:"Payment could not be initialized. Please try again.");setBusy(false)}}
 return <form onSubmit={submit}>{error&&<p className="form-message form-error full-field" role="alert">{error}</p>}<label>Full name *<input name="full_name" autoComplete="name" required/></label><label>Email address *<input type="email" name="email" autoComplete="email" required/></label><label>Phone number *<input type="tel" name="phone" autoComplete="tel" required/></label><label>Consultation method *<select name="consultation_type" required defaultValue=""><option value="" disabled>Select method</option><option value="video-call">Video call</option><option value="phone-call">Phone call</option><option value="whatsapp">WhatsApp</option></select></label><label>Preferred date<input type="date" name="preferred_date" min={new Date().toISOString().slice(0,10)}/></label><label>Preferred time<select name="preferred_time" defaultValue=""><option value="">Select time</option><option value="morning">Morning (9 AM–12 PM)</option><option value="afternoon">Afternoon (12–5 PM)</option><option value="evening">Evening (5–8 PM)</option><option value="flexible">Flexible</option></select></label><label className="full-field">Tell us about your project<textarea name="project_description" rows={5} placeholder="Your goals, features, audience, timeline and any specific requirements..."/></label><label className="full-field">Reference websites<textarea name="inspiration" rows={2} placeholder="Share any links or styles you like..."/></label><label>Session duration *<select name="session_duration" required value={duration} onChange={event=>setDuration(event.target.value)}><option value="" disabled>Select duration</option><option value="20">20 minutes</option><option value="40">40 minutes</option><option value="60">60 minutes</option></select></label><label>Price<div className="consultation-price"><span>₦</span><input value={prices[duration]??""} placeholder="Select duration to see price" readOnly aria-label="Consultation price"/></div></label><button className="primary-button consultation-submit" type="submit" disabled={busy}>{busy?"Opening secure checkout…":"Proceed"}<span>→</span></button></form>;
}
