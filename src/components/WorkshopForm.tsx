"use client";
import Link from "next/link";
import {FormEvent,useState} from "react";
import {ApiError,api} from "@/lib/api";
export default function WorkshopForm(){const[message,setMessage]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");setError("");try{const result=await api<{message:string}>("/workshops/20-days-with-wdn",{method:"POST",body:new FormData(event.currentTarget)});setMessage(result.message);event.currentTarget.reset()}catch(exception){setError(exception instanceof ApiError?Object.values(exception.errors).flat()[0]??exception.message:"Registration could not be submitted.")}finally{setBusy(false)}}return <form className="programme-form" onSubmit={submit}>{message&&<p className="form-message form-success">{message}</p>}{error&&<p className="form-message form-error">{error}</p>}<label>Full name<input name="full_name" required/></label><label>Email address<input name="email" type="email" required/></label><label>Phone number<input name="phone" type="tel" required/></label><label>Social media profile<input name="social_media_link" type="url" placeholder="https://" required/></label><label>LinkedIn profile<input name="linkedin_profile" type="url" placeholder="https://linkedin.com/in/..." required/></label><label>Payment receipt<input name="payment_receipt" type="file" accept="image/png,image/jpeg,application/pdf" required/></label><label className="affiliate-terms"><input name="agree_terms" type="checkbox" value="1" required/> I agree to the <Link href="/terms-of-service/#workshop-terms" target="_blank">workshop terms and conditions</Link>.</label><button disabled={busy}>{busy?"Submitting…":"Submit registration"} <span>→</span></button></form>}
