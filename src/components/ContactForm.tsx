"use client";
import {FormEvent,useState} from "react";
import {ApiError,api} from "@/lib/api";
export default function ContactForm(){const[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");setError("");const form=event.currentTarget,data=new FormData(form);try{const result=await api<{message:string}>("/contact",{method:"POST",body:JSON.stringify(Object.fromEntries(data.entries()))});setMessage(result.message);form.reset()}catch(exception){setError(exception instanceof ApiError?Object.values(exception.errors).flat()[0]??exception.message:"Your enquiry could not be sent.")}finally{setBusy(false)}}return <form onSubmit={submit}>{message&&<p className="form-message form-success full-field">{message}</p>}{error&&<p className="form-message form-error full-field">{error}</p>}<label>Name<input type="text" name="name" required/></label><label>Email<input type="email" name="email" required/></label><label>Phone<input type="tel" name="phone"/></label><label>What do you need?<select name="service" defaultValue=""><option value="" disabled>Select a service</option><option>Website Development</option><option>Ecommerce Website</option><option>Mobile App Development</option><option>Shopify Store Design</option><option>SEO</option><option>Social Media Management</option></select></label><label>Project details<textarea name="message" rows={6} required/></label><button className="primary-button contact-submit" type="submit" disabled={busy}>{busy?"Sending…":"Send Enquiry"} <span>→</span></button></form>}
