"use client";
import {FormEvent,useState} from "react";
import {useRouter,useSearchParams} from "next/navigation";
import {ApiError,api} from "@/lib/api";
export default function ResetPasswordForm(){const params=useSearchParams(),router=useRouter(),[busy,setBusy]=useState(false),[error,setError]=useState("");const token=params.get("token")??"",email=params.get("email")??"";async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);try{await api("/auth/reset-password",{method:"POST",body:JSON.stringify({token,email,password:form.get("password"),password_confirmation:form.get("password_confirmation")})});router.replace("/login/?reset=success")}catch(exception){setError(exception instanceof ApiError?Object.values(exception.errors).flat()[0]??exception.message:"This reset link is invalid or expired.")}finally{setBusy(false)}}if(!token||!email)return <p className="form-message form-error">This password reset link is incomplete or invalid.</p>;return <form className="auth-form" onSubmit={submit}>{error&&<p className="form-message form-error" role="alert">{error}</p>}<label>New Password<input type="password" name="password" autoComplete="new-password" minLength={8} required/></label><label>Confirm New Password<input type="password" name="password_confirmation" autoComplete="new-password" minLength={8} required/></label><button type="submit" disabled={busy}>{busy?"Resetting…":"Reset Password"}<span>→</span></button></form>}
