"use client";
import{FormEvent,useEffect,useState}from"react";import{useRouter}from"next/navigation";import{ApiError,api,getToken}from"@/lib/api";
export default function ChangePasswordForm(){const router=useRouter(),[busy,setBusy]=useState(false),[error,setError]=useState(""),[message,setMessage]=useState("");useEffect(()=>{if(!getToken())router.replace("/login/")},[router]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");setMessage("");const form=e.currentTarget,data=new FormData(form);try{const result=await api<{message:string}>("/auth/change-password",{method:"POST",body:JSON.stringify({current_password:data.get("current_password"),password:data.get("password"),password_confirmation:data.get("password_confirmation")})});setMessage(result.message);form.reset()}catch(err){setError(err instanceof ApiError?Object.values(err.errors).flat()[0]??err.message:"Password could not be changed.")}finally{setBusy(false)}}return <section className="dashboard-panel change-password-panel"><div className="dashboard-heading"><div><p className="section-label">Account security</p><h2>Choose a new password</h2></div></div>{message&&<p className="form-message form-success">{message}</p>}{error&&<p className="form-message form-error" role="alert">{error}</p>}<form className="change-password-form" onSubmit={submit}><label>Current Password<input type="password" name="current_password" autoComplete="current-password" required/></label><label>New Password<input type="password" name="password" autoComplete="new-password" minLength={8} required/></label><label>Confirm New Password<input type="password" name="password_confirmation" autoComplete="new-password" minLength={8} required/></label><button type="submit" disabled={busy}>{busy?"Changing password…":"Change Password"}<span>→</span></button></form></section>}
