import type{Metadata}from"next";import CourseCategoryFeed from"@/components/CourseCategoryFeed";import{ResourceFooter,ResourceHeader,ResourceHero}from"@/components/ResourceChrome";
const name=(slug:string)=>slug.split('-').map(x=>x[0]?.toUpperCase()+x.slice(1)).join(' ');
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;return{title:`${name(slug)} Courses`,description:`Explore practical ${name(slug)} courses from Website Designer Nigeria.`,alternates:{canonical:`/courses/${slug}/`}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;return <main className="resource-page collection-page"><ResourceHeader/><ResourceHero eyebrow="Online technology training" title={name(slug)} accent="courses that build skill." description="Learn through structured lessons, practical projects and expert support."/><section className="resource-light"><div className="shell"><CourseCategoryFeed slug={slug}/></div></section><ResourceFooter/></main>}
